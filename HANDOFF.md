# 월하잔향 프로젝트 인수인계

## 현재 상태

- 버전: `3.4.2`
- 공개 사이트: https://leejuhan-1214.github.io/Wolhajinhyang/
- GitHub 저장소: https://github.com/leejuhan-1214/Wolhajinhyang
- 구성: HTML/CSS/JavaScript 기반 브라우저 게임

## 바로 실행하기

Node.js 20 이상에서 프로젝트 폴더를 열고 다음 명령을 실행합니다.

```powershell
npm start
```

그다음 브라우저에서 `http://127.0.0.1:4173/`을 엽니다. 별도의 패키지 설치는 필요하지 않습니다.

## 검사하기

```powershell
npm test
```

## 주요 파일

- `index.html`: 게임 화면 및 UI 구조
- `styles.css`: 화면, 모바일 조작계, 편집기 스타일
- `game.js`: 게임 로직, 스테이지, 적과 보스, 관리자 기능
- `server.mjs`: 로컬 정적 서버
- `.github/workflows/pages.yml`: GitHub Pages 배포 설정
- `README.md`: 기능 및 조작법 전체 설명
- `STORY_SCRIPT.md`: 전체 줄거리, 인물 관계, 막별 주요 대사
- `VERSION.txt`: 현재 버전

## 관리자 모드

첫 화면의 난이도 버튼을 `병아리 → 신참내기 → 병아리 → 인간흉기` 순서로 클릭하면 관리자 모드가 활성화됩니다. 인증 진행 중간 문구는 표시되지 않으며, 성공했을 때만 활성화 상태가 표시됩니다.

관리자 모드에서는 첫 화면의 제목, 시작·이어하기 버튼, 네 난이도 이름과 인게임 맵 요소를 수정해 브라우저 저장소에 보존할 수 있습니다.

관리자 작전에서는 `L`로 전체 120구역 이동창을 열고, `Space` 상승·`Shift` 하강·`A/D` 좌우 이동으로 벽을 통과하며 2배속 비행합니다. `X`는 적·회복 아이템·도약 발판 생성, `Z`는 적과 회복 아이템·도약 발판의 저장 데이터까지 영구 삭제, `R`은 현재 위치에서 신참내기 규칙 전환입니다.

캠페인은 5개 스테이지, 120개 구역으로 구성되며 각 스테이지의 12번째 구역에 중간보스, 24번째 구역에 진 보스가 배치됩니다. 중간보스는 쇄우·적린·육화·공문·대역-13이고 진 보스는 철각·홍련·백면·무명·잔영-00입니다.

v2.0.1부터 방패병은 2칸 방패 내구도를 사용합니다. 정면 공격 두 번으로 3.2초간 가드 브레이크가 발생하고, 지상 3연타 마무리와 강화 공격은 방패를 관통합니다. 기본 체력·공격 빈도·철벽 호위망 피해 감소율도 함께 낮아졌습니다.

v2.1.5부터 전체 120개 구역에 관리자 편집 가능한 월드 표지판이 배치됩니다. 적린은 일본도로만 피해를 받는 반사 능력을 유지하면서 5발 산탄과 3발 정밀 사격을 사용합니다. 육화는 자신을 실제로 조준한 샷건만 감지하고, 0.12초 뒤 위치 교환 후 0.24초 동안 해당 산탄을 주인공에게 역류시켜 피해를 줍니다. v2.1.4부터 쇄우는 판넬을 사용하지 않으며 미사일·돌진·공중 포격·제압 사격만 사용합니다. 모든 방패병은 생성 경로나 스테이지에 관계없이 노란 방패로 2회를 막고, 소진되면 3.2초 동안 빨간 파괴 상태가 되어 공중 발도를 포함한 정면 공격에 피해를 받습니다. v2.1.3부터 일반 샷건 피해는 1.15, 산탄 수명은 0.30초이며 강화 샷건 피해는 2.2, 산탄 수명은 0.40초입니다. v2.1.2부터 튜토리얼과 초반 발도 안내에 적 총알 쳐내기 기능이 명시됩니다. 발도로 탄환을 제거하면 샷건 강화 게이지가 충전되며 공중에서는 이중 점프도 회복합니다. v2.1.1부터 공문의 검기·마구찌르기에 실제 칼 휘두르기 모션이 표시됩니다. 철각 판넬은 플레이어를 포위하지 않고 한쪽 원거리 편대에서만 발사하며, 강화 발도·충전 발도의 추가 피해와 강화 샷건의 피해·산탄 수가 하향 조정되었습니다. v2.1.0부터 적린은 일본도 전용 보스, 육화는 샷건 발사 반응 위치 교환 보스, 공문은 검기·마구찌르기 보스로 분리됩니다. 진 보스를 격파할 때마다 보상 레벨이 올라 최대 체력·탄창·공격력이 강화되며, 발판 내부에 끼인 적은 자동으로 위쪽 안전면으로 복구됩니다.

v2.0.0부터 맵은 480,000px로 확장됐고, 현재 구역 주변 적만 시뮬레이션해 장시간 플레이 성능을 유지합니다. 조작 가속·공중 제어·점프 입력 유예가 개선됐으며 스토리와 컷신 중 화면 흔들림은 발생하지 않습니다. 기존 12구역 저장은 같은 막 진행 비율을 유지해 24구역 구조로 변환됩니다.

## 배포

저장소의 `main` 브랜치 루트에 이 폴더의 파일을 올리면 GitHub Actions의 Pages 워크플로가 실행됩니다. 배포할 때 `.github`, `.nojekyll` 같은 숨김 파일도 함께 유지해야 합니다.

브라우저에 이전 버전이 남으면 배포 주소 뒤에 `?deploy=새커밋값`을 붙여 확인할 수 있습니다.
