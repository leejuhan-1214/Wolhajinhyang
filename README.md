# 월하잔향: 백야 작전 — v1.0.3

샷건 반동 이동과 발도술을 결합한 오리지널 2D 횡스크롤 액션 프로토타입입니다. 여섯 해 전에 사망한 노동자들의 구조 요청이 백야 폐기장에서 송신되고, 감찰요원 M-07은 중앙국의 소각 명령을 거부한 채 증거와 생존 신호를 지상으로 보내려 합니다.

## 실행

```powershell
node server.mjs
```

브라우저에서 `http://127.0.0.1:4173`을 열거나 `index.html`을 직접 실행하세요.

## GitHub에 올리기

이 폴더를 저장소 루트로 사용하세요. GitHub에서 빈 저장소를 만든 뒤 아래 명령을 실행하면 됩니다.

```powershell
git init
git add .
git commit -m "Initial release"
git branch -M main
git remote add origin https://github.com/사용자명/저장소명.git
git push -u origin main
```

저장소의 `Settings → Pages → Build and deployment → Source`를 `GitHub Actions`로 선택하면 `main` 브랜치에 푸시할 때 게임이 자동 배포됩니다. 배포 주소는 Actions의 `Deploy GitHub Pages` 작업에서 확인할 수 있습니다.

로컬 실행과 문법 검사는 다음 명령으로 할 수 있습니다.

```powershell
npm.cmd start
npm.cmd test
```

## 조작

- `A`, `D`: 이동
- `Space`: 점프 / 이중 점프 / 벽차기
- 마우스 이동: 조준
- 마우스 왼쪽: 캐릭터가 바라보는 방향으로 발도술
- 마우스 오른쪽: 샷건. 공중에서 아래로 발사하면 반동으로 상승
- `F` 또는 `C`: 키보드 샷건 보조 입력
- `J` 또는 `X`: 키보드 발도 보조 입력
- `E`: 주변 탄환을 소거하는 버스트
- `R`: 마지막 체크포인트 복귀
- `Esc`: 일시정지

## 전투 순환

발도술을 적중시키거나 적 탄환을 베면 샷건 강화 게이지가 충전됩니다. 3칸이 가득 찬 상태에서 발사하면 강화 샷건이 나가며 방패를 관통하고 넓은 범위에 큰 피해를 줍니다.

샷건은 두 발을 순차 재장전합니다. 공중에서 아래쪽을 조준해 발사하면 반동으로 다시 상승하고, 수평으로 발사하면 반대 방향으로 빠르게 밀려납니다. 이중 점프를 사용한 뒤 공중 발도를 맞히면 추가 점프가 돌아옵니다.

## v1.0 전면 개편

- v1.0.3: 사람 주인공의 팔꿈치·무릎·발목을 분리한 관절형 픽셀 애니메이션과 달리기·도약·낙하·벽 매달리기 포즈 추가
- v1.0.3: 로봇 적과 보스의 2단 관절 보행, 피스톤, 케이블, 리벳, 발광 패널과 세부 장갑 표현 추가

- 왼쪽 클릭 발도 / 오른쪽 클릭 샷건 입력 체계
- 마우스 조준선과 전용 조준점
- 발도 적중으로 충전하는 3단계 강화 샷건
- 샷건 반동을 이용한 공중 체공과 간격 이동
- 고속 이동을 작은 단계로 계산하는 플레이어·탄환 충돌
- 모든 적 탄환의 지형 충돌. 벽을 무시하던 관통탄 제거
- 지상 적의 중력·발판·벽 충돌과 적끼리 겹침 분리
- 보스의 낭떠러지 감지·도약 판단과 결전장 복귀 안전장치
- 반복형이던 기존 레벨을 폐기하고 새로 배치한 36,000px / 12구역 맵
- 백야 폐기장, 검은 공장, 지하 피난선으로 이어지는 새로운 3막 스토리
- 샷건 반동 점프를 요구하는 중앙 공백, 수직벽, 레이저 구간과 3개 봉쇄 전투실
- 근접병, 방패병, 사수, 박격포병, 비행 감시기, 중간 보스와 최종 보스

모든 캐릭터·배경·대사·맵 배치는 이 프로토타입을 위해 새로 만든 오리지널 콘텐츠이며, 그래픽은 외부 이미지 없이 Canvas 도형으로 렌더링합니다.
